# OSHA Wally Trial - Production .env template
# Copy this file to ".env" in the repo root and set real values.
# This is the single supported production setup for Task Scheduler runs.

# Required SMTP settings
SMTP_HOST=smtppro.zoho.com
SMTP_PORT=465
SMTP_USER=alerts@yourdomain.com
SMTP_PASS=your-app-password

# Required branding/compliance
BRAND_NAME=YourBrand
MAILING_ADDRESS=123 Main St, City, ST 12345

# Optional legal display name (only if used publicly)
# BRAND_LEGAL_NAME=Your Brand LLC

# Optional sender identity overrides (if applicable)
# FROM_EMAIL=alerts@yourdomain.com
# FROM_NAME=Your Brand OSHA Alerts

# Optional one-click unsubscribe endpoint
# If set, List-Unsubscribe includes both mailto and one-click URL.
# If unset, List-Unsubscribe uses mailto only.
# UNSUB_ENDPOINT_BASE=https://yourdomain.com/unsubscribe
# UNSUB_SECRET=long-random-secret-required-when-unsub-endpoint-enabled
