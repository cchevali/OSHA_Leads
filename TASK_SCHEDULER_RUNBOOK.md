# Deprecated: Task Scheduler Runbook

This document is deprecated. Use `WALLY_TRIAL_RUNBOOK.md` as the canonical
reference for Task Scheduler setup and verification. It includes the exact
action string, the `--enable-schedule` creation flow, and the
`--check-schedule` verification step.
