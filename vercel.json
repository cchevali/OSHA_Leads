{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "nextjs",
  "installCommand": "[ -d web ] && cd web; npm ci",
  "buildCommand": "[ -d web ] && cd web; npm run build",
  "devCommand": "[ -d web ] && cd web; npm run dev"
}
